export enum IssuesStatus {
  TODO = 'todo',
  IN_PROGRESS = 'in progress',
  DONE = 'done'
}

export enum Status {
  LOADING = 'loading',
  SUCCESS = 'loaded',
  ERROR = 'error'
}

export enum Errors {
  INVALID_PROJECT_NAME = 'invalid project name',
  INVALID_GITHUB_REPO = 'invalid github repo'
}
